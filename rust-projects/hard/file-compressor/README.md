# File-Compressor

## 📖 Overview

This is a **Hard**-level **Backend / CLI** application crafted using **Rust**. It explores core concepts, architectural patterns, and practical execution within this technology stack. 

## 🚀 Execution

`cargo run` to execute or `cargo build --release` for a production binary.
