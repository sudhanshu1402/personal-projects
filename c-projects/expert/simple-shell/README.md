# Simple Shell

## 📖 Overview

This is a **Expert**-level **Backend / CLI** application crafted using **C**. It explores core concepts, architectural patterns, and practical execution within this technology stack. 

## 🚀 Execution

`gcc` or `g++` to compile the source code, then execute the output binary (e.g., `./a.out`).
