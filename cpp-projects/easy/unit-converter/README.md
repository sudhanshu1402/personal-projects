# Unit Converter

## 📖 Overview

This is a **Easy**-level **Backend / CLI** application crafted using **Cpp**. It explores core concepts, architectural patterns, and practical execution within this technology stack. 

## 🚀 Execution

`gcc` or `g++` to compile the source code, then execute the output binary (e.g., `./a.out`).
