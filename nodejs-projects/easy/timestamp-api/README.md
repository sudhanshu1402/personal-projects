# Timestamp API

## 📖 Overview

This is a **Easy**-level **Backend / CLI** application crafted using **Nodejs**. It explores core concepts, architectural patterns, and practical execution within this technology stack. 

## 🚀 Execution

`npm install` then `node index.js` or `npm start`.
