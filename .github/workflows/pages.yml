name: Deploy Static Frontends to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Isolate Frontend Projects
        run: |
          mkdir _gh_pages
          # Copy the JS projects preserving structure
          cp -r javascript-projects _gh_pages/
          # Copy the stunning new Web UI directory portal
          cp index.html _gh_pages/
          cp projects_data.js _gh_pages/
          # Optional fallback
          cp README.md _gh_pages/ || true
          
      - name: Setup Pages
        uses: actions/configure-pages@v5
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_gh_pages'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
